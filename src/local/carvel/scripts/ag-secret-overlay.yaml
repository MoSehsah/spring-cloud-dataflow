#@ load("@ytt:overlay", "overlay")

#! #@overlay/match by=overlay.subset({"kind":"ServiceAccount","metadata":{"namespace":"secretgen-controller", "name":"secretgen-controller-sa"}}),expects="1+"
#@overlay/match by=overlay.subset({"kind":"ServiceAccount"}),expects="1+"

---
#@overlay/match missing_ok=True
imagePullSecrets:
  - name: reg-creds-dev-registry
